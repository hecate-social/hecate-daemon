{erl_opts, [debug_info]}.

{deps, [
    %% Macula mesh client
    {macula, "0.20.5"},

    %% JSON encoding
    {jsx, "3.1.0"},

    %% Domain app (for event types)
    {guide_node_lifecycle, {path, "../guide_node_lifecycle"}}
]}.

{shell, [{apps, [hecate_mesh]}]}.

{erl_opts, [debug_info]}.

{deps, [
    %% Macula mesh client
    {macula, "0.20.9"},

    %% Shared utilities (for hecate_api_utils in call_rpc_api)
    {shared, {path, "../shared"}}
]}.

{src_dirs, [
    "src",
    "src/call_rpc"
]}.

{shell, [{apps, [hecate_mesh]}]}.

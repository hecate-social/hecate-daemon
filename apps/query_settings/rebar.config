{erl_opts, [debug_info]}.

{deps, [
    {project_settings, {path, "../project_settings"}}
]}.

%% Include vertical slice subdirectories
{src_dirs, [
    "src",
    "src/get_settings",
    "src/get_identity"
]}.

{shell, [{apps, [query_settings]}]}.
